# Intelligent Article-to-Image Web App

A full-stack AI application that transforms articles (text, URLs, files) into stunning visuals using an intelligent agent pipeline.

## 🏗 Project Structure

```
/article-to-image-app
├── /backend                 # Node.js + Express Backend
│   ├── /src
│   │   ├── /agent          # AI Agent Modules (The Brain)
│   │   │   ├── inputProcessor.js
│   │   │   ├── semanticEngine.js
│   │   │   ├── visualPlanner.js
│   │   │   ├── promptGenerator.js
│   │   │   ├── imageGenerator.js
│   │   │   ├── clipEvaluator.js
│   │   │   └── agentFlow.js
│   │   ├── /api            # API Routes
│   │   └── server.js       # Entry Point
│   └── .env.example        # Env Config
│
└── /frontend               # React + Vite Frontend
    ├── /src
    │   ├── /components/ui  # Shadcn UI Components
    │   ├── /pages          # Application Pages
    │   ├── /store          # Zustand State Management
    │   └── App.jsx
    └── tailwind.config.js
```

## 🚀 Getting Started

### Prerequisites

-   Node.js (v16+)
-   npm or yarn

### Backend Setup

1. Navigate to backend:
    ```bash
    cd backend
    ```
2. Install dependencies:
    ```bash
    npm install
    ```
3. Configure Environment:
   Copy `.env.example` to `.env` and add your OpenAI API Key (optional for demo mode).
    ```bash
    cp .env.example .env
    ```
4. Start Server:
    ```bash
    npm start
    ```
    Server runs on `http://localhost:3000`

### Frontend Setup

1. Navigate to frontend:
    ```bash
    cd frontend
    ```
2. Install dependencies:
    ```bash
    npm install
    ```
3. Start Dev Server:
    ```bash
    npm run dev
    ```
    App runs on `http://localhost:5173`

## 🤖 AI Agent Workflow

1.  **Input Processing**: Cleans text, scrapes URLs, or extracts from PDFs.
2.  **Semantic Understanding**: Analyzes sentiment, keywords, and summarizes content.
3.  **Visual Planning**: Decides on art style, lighting, and composition based on sentiment.
4.  **Prompt Engineering**: Generates a high-fidelity prompt for the image model.
5.  **Generation**: Calls AI Model (Mocked/OpenAI) to create 4 variations.
6.  **Evaluation**: Ranks images using CLIP scoring (Simulated).

## 📡 API Endpoints

-   `POST /api/extract`: Parse input (Text/URL/File)
-   `POST /api/generate`: Run the Agent Pipeline
-   `GET /api/history`: View generation history

## 🛠 Tech Stack

-   **Frontend**: React, Vite, TailwindCSS, Zustand, Lucide Icons
-   **Backend**: Express, Cheerio (Scraping), PDF-Parse, Mammoth
-   **AI/Agent**: Custom modular pipeline

---

_Generated by Cascade AI_
